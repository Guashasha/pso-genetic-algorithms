use crate::EvolutionStats;

pub fn evolve(population: Vec<Vec<f64>>) -> EvolutionStats {
    todo!();
}

fn merge(father1: &[f64], father2: &[f64]) -> Vec<f64> {
    todo!();
}

fn mutate(individual: &mut [f64]) {
    todo!();
}
